import StyledButtonIcon from "./StyledButtonIcon";
import { PlayIcon } from "./StyledIcon";

const PlayButton = (props) => (
  <StyledButtonIcon {...props}>
    <PlayIcon />
  </StyledButtonIcon>
);

export default PlayButton;
