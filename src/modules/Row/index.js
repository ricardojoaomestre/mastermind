import { HintPin, Pin } from "../../components/Pin";

import {
  StyledRowContainer,
  StyledRowNumber,
  StyledGuessContainer,
  StyledHintContainer,
} from "./Row.style";

const Row = (props) => {
  return <></>;
};
export default Row;
